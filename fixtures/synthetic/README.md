# Synthetic test fixtures ONLY

All products, ASINs, URLs and ranks in this directory are fabricated for automated tests. Never import into a production project or application bundle. Test sources and categories are activated only inside isolated tests.
